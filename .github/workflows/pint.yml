name: Laravel Pint

on:
  workflow_dispatch:  # Manual only — does not run automatically

permissions:
  pull-requests: write

jobs:
  uneditable:
    uses: trainivel/.github/.github/workflows/pint.yml@main
